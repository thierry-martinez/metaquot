(library
  (public_name metaquot)
  (preprocess (pps metapp metapp.ppx))
; -warning 40: Constructor or label name used out of scope. (OCaml <=4.06.0)
  (flags -w -40)
  (libraries compiler-libs metapp stdcompat))
